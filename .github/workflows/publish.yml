name: publish

on:
    workflow_dispatch:
jobs:
   publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: publish
        env:
            PSGALLERY_KEY: ${{ secrets.PSGALLERY_KEY }}
        shell: pwsh
        run: |
          Publish-Module -Path ./Module/UUPDumpDownloader.psd1 -NuGetApiKey $env:PSGALLERY_KEY -Verbose
